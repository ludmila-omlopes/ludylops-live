# Implementation Plans

## Direção atual — revisão de 2026-09-15

Objetivo: cada streamer ter sua própria comunidade, usando o mesmo motor.
Ludylops é a primeira instância, conforme o plano 020. O marco de produto é
um segundo streamer conseguir criar, configurar e operar a comunidade sem
editar código, copiar banco ou acessar dados de outra comunidade.

Os planos 008–019 foram reconciliados com o código em `ec19f8f`, cuja árvore
de arquivos é igual à do master remoto `f353ce2`. Esta revisão atualiza
documentos e os corpos das issues existentes; não executa implementação,
migração, deploy ou abertura de novas issues. Os números dos planos e das
issues são preservados. Datas e resultados de testes antigos abaixo são
históricos, não uma validação executada nesta revisão.

A separação de layouts do plano 021 já foi integrada no PR #187. Os arquivos
atuais estão em src/app/(builder), src/app/(community),
src/app/(creator-public) e src/app/obs; as URLs públicas foram preservadas.
Essa organização não constitui isolamento de dados nem gestão por streamer.

### Ordem recomendada

1. Base: 014 (#180), 015 (#181) e 017 (#183), independentes entre si.
2. Após 014, preparar 008 (#172); após 014 e 017, preparar 018 (#184).
3. Após 008, 017 e 018, executar o piloto técnico 009 (#173).
4. Aplicar 019 (#185), começando pelas ações isoladas do piloto e bloqueando
   ações ainda globais para outros criadores. Completar as lacunas de produto
   listadas abaixo antes de declarar o beta externo pronto.
5. 016 (#182), após 017, entrega somente a raiz dos subdomínios. Um piloto por
   caminho /c/{slug} não depende dessa entrega, desde que suas próprias rotas e
   requisições mantenham contexto validado.
6. 010 (#175) e 012 (#177) são melhorias auxiliares; 011 (#176) depende da
   separação real das consultas selecionadas. 013 (#178) foi reduzido a uma
   medição curta; uma comparação ampla de transportes fica adiada.

### Lacunas de produto ainda sem planos/issues próprios

- Gestão pelo dono da comunidade: associação usuário/comunidade e permissões
  próprias; /owner continua sendo o console da operadora. Não conceder acesso
  global de admin para contornar essa falta.
- Edição e consumo da configuração: identidade, canal/links, navegação,
  metadados, moeda, pricing, estado da live e configuração dos overlays.
- Migrações restantes: economia, apostas, sugestões/produtos, catálogo/resgates,
  credenciais e filas da bridge, contadores e configurações por criador.
- Preparação/publicação mínima e validação com duas comunidades em banco de
  teste, incluindo leituras, escritas, permissões e integrações.

Quotes já depende de cobrança, reembolso e estado da live. O piloto técnico
pode isolar ações menores, mas deve recusar ações de outro criador que ainda
dependam desses dados globais. Terminar 008/009 não equivale a concluir o produto.
Essas lacunas são registradas como dependências de liberação; esta revisão
não inventa planos numerados nem implementa funcionalidades adicionais.

Generated by the improve skill on 2026-06-11, at commit `06f0792`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Verification baseline at `06f0792`: `npm run lint`, `npx tsc --noEmit`, `npm test`
(28 files / 196 tests) and `npm run build` all pass.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Issue | Status |
|------|-------|----------|--------|------------|-------|--------|
| 001  | CI verification baseline (lint, typecheck, tests, build) | P1 | S | — | [#136](https://github.com/ludmila-omlopes/ludylops-live/issues/136) | DONE (PR #143) |
| 002  | Fail closed in production when auth-critical env vars missing | P1 | S | 001 | [#137](https://github.com/ludmila-omlopes/ludylops-live/issues/137) | DONE |
| 003  | Route-level tests for bridge + Streamer.bot endpoints | P1 | M | 001 | [#138](https://github.com/ludmila-omlopes/ludylops-live/issues/138) | DONE |
| 004  | Bridge redemption claim/complete/fail idempotency | P1 | M | 003 | [#139](https://github.com/ludmila-omlopes/ludylops-live/issues/139) | DONE |
| 005  | redeemItem balance overdraw + stock oversell guards | P1 | S | 001 | [#140](https://github.com/ludmila-omlopes/ludylops-live/issues/140) | DONE |
| 006  | Hardening batch: timing-safe sync auth, dep vulns, FK indexes | P2 | M | 001 | [#141](https://github.com/ludmila-omlopes/ludylops-live/issues/141) | DONE (manual index migration generated; not applied) |
| 007  | Public landing page for the creator platform beta at `/criar-area` | P1 | M | white-label foundation committed (see plan) | [#169](https://github.com/ludmila-omlopes/ludylops-live/issues/169) | DONE ([PR #171](https://github.com/ludmila-omlopes/ludylops-live/pull/171) merged 2026-07-07) |
| 008  | Add `creator_id` to operational tables (schema + backfill, no behavior change) | P1 | M | 014 | [#172](https://github.com/ludmila-omlopes/ludylops-live/issues/172) | IN PROGRESS ([PR #194](https://github.com/ludmila-omlopes/ludylops-live/pull/194) brings #193 to master; verified on disposable PostgreSQL) |
| 009  | Thread creator context + scope the quotes vertical (pilot + pattern doc) | P1 | L | 008, 017, 018 | [#173](https://github.com/ludmila-omlopes/ludylops-live/issues/173) | TODO |
| 010  | Cut OBS overlay polling cost (intervals + live gating) | P2 | S | — | [#175](https://github.com/ludmila-omlopes/ludylops-live/issues/175) | TODO |
| 011  | Cache isolated shared reads with required creator keys | P2 | M | 009 + selected loaders isolated (unnumbered follow-ups) | [#176](https://github.com/ludmila-omlopes/ludylops-live/issues/176) | TODO |
| 012  | Bound hot queries + HLTB refresh off request path | P2 | M | — | [#177](https://github.com/ludmila-omlopes/ludylops-live/issues/177) | TODO |
| 013  | Measure overlay cost before a realtime redesign | P3 | S | pilot observations when available | [#178](https://github.com/ludmila-omlopes/ludylops-live/issues/178) | TODO |
| 014  | Establish a safe Drizzle migration and data-seed baseline | P1 | M | — | [#180](https://github.com/ludmila-omlopes/ludylops-live/issues/180) | DONE ([PR #190](https://github.com/ludmila-omlopes/ludylops-live/pull/190) integrated in master; no shared DB changes) |
| 015  | Harden creator-area creation errors and concurrent slug conflicts | P1 | S | — | [#181](https://github.com/ludmila-omlopes/ludylops-live/issues/181) | DONE ([PR #191](https://github.com/ludmila-omlopes/ludylops-live/pull/191) integrated in master; 437 tests after conflict resolution) |
| 016  | Route creator subdomains to their public Next.js surface | P1 | M | 017 | [#182](https://github.com/ludmila-omlopes/ludylops-live/issues/182) | TODO |
| 017  | Make creator lifecycle status authoritative at public boundaries | P1 | M | — | [#183](https://github.com/ludmila-omlopes/ludylops-live/issues/183) | DONE ([PR #192](https://github.com/ludmila-omlopes/ludylops-live/pull/192) integrated in master) |
| 018  | Bind Streamer.bot requests to per-creator credentials | P1 | L | 014, 017 | [#184](https://github.com/ludmila-omlopes/ludylops-live/issues/184) | IN PROGRESS ([PR #195](https://github.com/ludmila-omlopes/ludylops-live/pull/195); 496 tests, types, lint, build, PostgreSQL and C# checks passed; integrate #194 first) |
| 019  | Enforce module authorization and dependency-safe transitions | P1 | L | 009, 017, 018 | [#185](https://github.com/ludmila-omlopes/ludylops-live/issues/185) | TODO |
| 020  | Define builder, shared engine and Ludylops instance boundaries | P1 | M design / L migration | none for design; reconcile 008–019 before migration | — | DONE (architecture direction recorded; migration remains staged in follow-ups) |
| 021  | Separate builder, community, public creator and OBS layouts, preserving routes | P1 | M | direction in 020; no database migration | — | DONE (integrated via PR #187; commits 8f24f46, 6b5042b; historical inventory: 21 pages / 78 APIs) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001 because production builds will require `NEXTAUTH_SECRET`, and 001's CI workflow provides the dummy value for the build step.
- 004 requires 003 because the route tests characterize the HTTP contract that 004's repository changes must not break.
- 005 and 006 only need 001 (gates in CI); they are independent of each other and of 003/004.
- 006 generates a Drizzle migration but does NOT apply it — applying to the Neon database (`npm run db:push`) is an operator decision.
- 007 (added 2026-07-07 at `aa9104c`) depends on the white-label foundation files (`src/lib/creators/access.ts`, `src/app/(viewer)/criar-area/**`, creator forms) being committed and merged — they were untracked working-tree files when the plan was written. Its drift check verifies this. **DONE** (merged in PR #171).
- 014 must precede 008 because the documented `db:push` flow does not execute data seeds embedded only in generated SQL; 008's foreign-keyed backfill requires `creator_ludylops` to exist first.
- 009 requires 008, 017 and 018: schema groundwork, strict public lifecycle/context, and authenticated integration context. Paid quote display/refund and live state have additional unnumbered release dependencies. 008 excludes the quote tables; 009 owns their constraints.
- 016 requires 017 so wiring wildcard hostnames cannot newly expose disabled or archived creators.
- 018 blocks non-default legacy actions before side effects; 009 can release only verified scoped quote actions. 019 later adds the shared dependency policy and a coverage matrix. This ordering avoids a circular 009/018/019 dependency while keeping unscoped operations unavailable.
- 010 and 012 can improve current operation independently, while preserving any creator context introduced by isolation work. 011 requires 009's context convention and actual isolation of every selected loader: creator-aware keys cannot fix global queries. 013 is a bounded measurement task, not an external-beta prerequisite.

## Multi-tenant isolation roadmap (creator_id scoping)

The white-label foundation creates creator instances, but operational data is still single-tenant. Isolation is a phased program, tracked here so the shape and remaining cost stay visible:

- **008 (IN PROGRESS; PR #194)** — schema groundwork: `creator_id` on the 16 surrogate-PK operational tables, backfilled to `creator_ludylops`. Disposable PostgreSQL checks and application checks passed. PR #193 was merged into the former #190 branch after #190 reached master; #194 integrates the remaining changes. Apply schema before deploying the updated application. Queries remain unscoped.
- **009 (TODO)** — pilot: prove the request→repository threading pattern and per-creator sequence/constraint handling on the **quotes** vertical; produce `docs/creator-scoping.md`.
- **Remaining per-module plans (not yet numbered)** — replicate 009's pattern per vertical, each depending on 009 and following the pattern doc:
  - bets (`bets`/`bet_options`/`bet_entries`)
  - suggestions (`game_suggestions`/`video_suggestions`/`creator_suggestions` + boosts) and `product_recommendations`
  - redemptions (`redemptions`) + bridge
  - economy (`point_ledger`, and `viewer_balances` — needs composite PK `(viewer_id, creator_id)`, per the confirmed **per-creator balance** decision)
  - catalog (`catalog_items` — `slug` unique must become composite `(creator_id, slug)`)
  - counters/overlays (`streamerbot_counters` — key-based **and** dual-used by creator-area-access settings in `src/lib/creators/access.ts`; handle carefully)
  - Identity stays global (not scoped): `users`, `google_accounts`, `google_account_viewers`, `viewer_links`; shared reference/infra stays global: `ps_plus_catalog_*`, `google_risc_deliveries`.
- **Cleanup (deferred)** — once all inserts pass `creator_id` explicitly, drop the temporary column defaults so a missing `creator_id` fails loudly instead of silently defaulting to the Ludylops tenant.

Confirmed product decisions (2026-07-07): pipetz balances are **per-creator** (independent economy per instance); child/activity tables get a **denormalized `creator_id`** (single-column scoping, no joins).

## Findings considered and rejected

So nobody re-audits these:

- **Boost/bet double-spend race** (`boostGameSuggestion`, `placeBet`): not a bug — the debit uses a `gte`-guarded update with `.returning()` inside a transaction; zero-row → throw → rollback. This is the *correct* pattern (and the exemplar plans 004/005 copy).
- **HMAC verified over raw body then `JSON.parse` of the same string**: that is the textbook-correct order of operations, not a vulnerability.
- **CSRF on admin GET routes**: CSRF cannot read responses; the GETs require an admin session. No exploit path evidenced.
- **5-minute HMAC replay window** (`src/lib/streamerbot/security.ts:3`): industry-standard tolerance (Stripe uses the same); not worth tightening given Streamer.bot clock skew risk.
- **IDOR on `/api/viewers/[youtubeChannelId]`**: the DB path selects only public profile + balance fields, and balances are public by design (ranking page). Demo path returns demo data only.
- **Demo-store `structuredClone` cost**: demo mode only; negligible.
- **OBS overlay polling → serverless/Neon cost**: real but architectural (SSE/WebSocket redesign); deferred — revisit if Neon/Vercel bills climb.
- **`repository.ts` decomposition (10,033-line god module, duplicated demo/DB paths)**: real and HIGH-impact, but deliberately not planned this round — user selected the top-5 + hardening batch. Decompose only after 003/004/005 land (tests first). Re-raise next run.
- **Unbounded admin queries / O(n×m) suggestion assembly**: real, MED impact; not selected this round. Re-raise next run.
- **Beta email gating for creator-area creation** (requested 2026-07-07): already fully implemented — `canCreateCreatorArea` in `src/lib/creators/access.ts` (admin emails + admin-managed allowlist), enforced on the `/criar-area` page and on `GET/POST /api/me/creator-area`, with admin UI (`admin-creator-area-access-panel.tsx`) and tests (`access.test.ts`). No plan needed; only the landing page (007) was missing.

## Issues

Planos 008–019 correspondem às issues #172, #173, #175–#178 e #180–#185.
Nesta revisão, os corpos dessas 12 issues são sincronizados integralmente a
partir dos planos locais, preservando títulos, labels e estado. O índice usa
a prioridade revisada dos planos; labels históricos não são reclassificados.
Issues #6, #22, #28 e #165 não têm um plano numerado neste conjunto e ficam
inalteradas nesta etapa.
